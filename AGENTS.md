# Skill 开发最佳实践

## 凭证与环境变量管理

### 规范
- 支持通过环境变量配置外部服务接口（如 `OPENAI_BASE_URL`、`OPENAI_API_KEY`）
- 脚本启动时自动读取当前 skill 目录下的 `.env`（如果存在），仅在对应环境变量**尚未设置**时作为默认值注入
- 环境变量优先级：已有环境变量 > `.env` 文件 > 默认值
- 如果工具支持交互式弹窗（如 `AskUserQuestion`），变量优先使用交互式弹窗交互，降低用户使用门槛

### 落盘流程
当用户在当前会话里首次提供一套可复用配置时：
1. 一次性询问所有必填字段（如 Base URL、API Key 等），不要分多次询问
2. 最后询问是否写入当前 skill 目录下的 `.env`
3. 只有在用户同意后，才可以落盘保存
4. 写入时避免覆盖已有配置项

### 适用场景
所有涉及外部 API 凭证、密钥、endpoint 的 skill 都应遵循此模式，减少用户重复输入。
